<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Telemedicine Application</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #e9ecef;
            margin: 0;
        }

        /* Header */
        header {
            background: linear-gradient(45deg, #007bff, #00b4d8);
            color: white;
            padding: 20px 0;
            position: sticky;
            top: 0;
            z-index: 1000;
            text-align: center;
        }

        header h1 {
            margin: 0;
            font-size: 2.5rem;
            text-shadow: 2px 2px 4px rgba(68, 130, 212, 0.2);
        }

        /* Hero Section */
        .hero {
            background: linear-gradient(45deg, rgba(0, 123, 255, 0.8), rgba(0, 180, 216, 0.8)), url('https://source.unsplash.com/1600x900/?healthcare,medicine') no-repeat center center;
            background-size: cover;
            height: 450px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #f5f7fa;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .hero h2 {
            font-size: 3rem;
            font-weight: 600;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.4);
            animation: fadeInDown 1.5s ease-out;
        }

        .hero p {
            font-size: 1.2rem;
            color: #fff;
            animation: fadeInUp 1.5s ease-out;
        }

        /* Button Styles */
        .nav-buttons {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }

        .nav-buttons a {
            padding: 15px 30px;
            font-size: 18px;
            font-weight: 600;
            color: #fff;
            background-color: #007bff;
            border-radius: 8px;
            text-decoration: none;
            transition: background-color 0.3s, transform 0.2s;
            display: flex;
            align-items: center;
            gap: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .nav-buttons a:hover {
            background-color: #0056b3;
            transform: scale(1.05);
        }

        /* Quick Links */
        .quick-links {
            margin-top: 40px;
            text-align: center;
        }

        .quick-links h2 {
            margin-bottom: 20px;
            color: #007bff;
            font-weight: bold;
        }

        .links-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
        }

        .quick-link {
            display: flex;
            align-items: center;
            background-color: #f8f9fa;
            border-radius: 10px;
            padding: 15px;
            text-decoration: none;
            color: #333;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s;
        }

        .quick-link:hover {
            transform: scale(1.05);
            background-color: #e0f7ff;
        }

        .link-icon {
            font-size: 24px;
            margin-right: 10px;
        }

        /* Map */
        #map {
            height: 400px;
            width: 100%;
            margin-top: 20px;
        }

        /* Fade In Animations */
        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Telemedicine Application</h1>
    </header>

    <div class="hero">
        <div class="hero-content">
            <h2>Welcome to Our Telemedicine Platform</h2>
            <p>Connecting patients and healthcare professionals for better health outcomes.</p>
        </div>
    </div>

    <div class="container text-center mt-4">
        <div class="nav-buttons">
            <a href="/login"><i class="fas fa-user"></i> Patient Login</a>
            <a href="/register"><i class="fas fa-user-plus"></i> Register</a>
            <a href="/profile"><i class="fas fa-id-badge"></i> Profile</a>
            <a href="/admin/dashboard"><i class="fas fa-user-shield"></i> Admin Dashboard</a>
            <a href="/admin/login"><i class="fas fa-sign-in-alt"></i> Admin Login</a>
            <a href="/doctors/login"><i class="fas fa-stethoscope"></i> Doctors Login</a>
        </div>
    </div>

    <div class="quick-links">
        <h2>Quick Links</h2>
        <div class="links-container">
            <a href="/appointments/book" class="quick-link">
                <div class="link-icon">📅</div>
                <div>Schedule an Appointment</div>
            </a>
            <a href="/doctors" class="quick-link">
                <div class="link-icon">🔍</div>
                <div>Find a Doctor</div>
            </a>
            <a href="/resources" class="quick-link">
                <div class="link-icon">📚</div>
                <div>Health Resources</div>
            </a>
        </div>
    </div>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8">
                <h2>Our Services</h2>
                <ul>
                    <li>Virtual Consultations</li>
                    <li>Remote Health Monitoring</li>
                    <li>Prescription Refills</li>
                    <li>Medical History Review</li>
                </ul>

                <!-- Google Maps (Leaflet) -->
                <div id="map"></div>
            </div>
        </div>
    </div>

    <footer>
        <p>&copy; 2024 Telemedicine Application. All rights reserved.</p>
    </footer>

    <!-- Leaflet Script -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
    <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
    <script>
        const map = L.map('map').setView([-1.286389, 36.817223], 10); // Nairobi, Kenya
        L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
            maxZoom: 19,
        }).addTo(map);
    </script>
</body>

</html>
